import LineForm from "./line.form";

const AdminComponent = () => {
  /* const [startDate, setStartDate] = useState(new Date()); */

  return <LineForm mode="add" />;
};

export default AdminComponent;
